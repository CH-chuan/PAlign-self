# Model-to-GPU-count mapping.
# Format: pattern:gpu_count
# Patterns are matched case-insensitively against the model name.
# For HuggingFace snapshot paths, the model name is extracted first.
# Default: 1 GPU if no pattern matches.
gpt-oss-120b:2
Meta-Llama-3-8B:1
qwen3-coder-30b-a3b:1
